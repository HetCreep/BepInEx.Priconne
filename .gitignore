# Agent-local files (private; governance lives at the Modloader hub, not here)
.claude/
AGENTS.md
MEMORY.md

# Priconne dev components — checked out here as git worktrees of this repo's own branches
# (Il2CppInterop / Il2CppDumper / PrincessDumper). Each is a separate working tree; main must
# not track them. (Upstream dumper source clones were moved to ../_upstream-dumper-clones/.)
Il2CppInterop/
Il2CppDumper/
PrincessDumper/

# Build output
[Bb]in/
[Oo]bj/
*.user

# Local NuGet restore
packages/

# Game-derived / large local artifacts (never commit; offline-generated, shipped via Releases)
dist/
interop/
runtime-dump/
*.dat
global-metadata*.dat
