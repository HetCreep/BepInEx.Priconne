# Agent-local files (private; governance lives at the Modloader hub, not here)
.claude/
AGENTS.md
MEMORY.md

# The 4 component branches — checked out here as git worktrees of this repo's own branches.
# The default branch (main) is the HUB (docs + the download/assembly point); the 4 components
# (BepInEx / Il2CppInterop / Il2CppDumper / PrincessDumper) each live on a sibling branch and
# are mounted here as separate working trees. main must NOT track them.
# (Upstream dumper source clones were moved to ../_upstream-dumper-clones/.)
BepInEx/
Il2CppInterop/
Il2CppDumper/
PrincessDumper/

# Build output
[Bb]in/
[Oo]bj/
*.user

# Local NuGet restore
packages/

# Game-derived / large local artifacts (never commit; offline-generated, shipped via Releases)
dist/
interop/
runtime-dump/
*.dat
global-metadata*.dat
